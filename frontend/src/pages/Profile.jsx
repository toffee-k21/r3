import React from "react";
import Logout from "../components/Logout";

const Profile = () => {
  return (
    <div>
      <div>Profile</div>
      <Logout />
    </div>
  );
};

export default Profile;
